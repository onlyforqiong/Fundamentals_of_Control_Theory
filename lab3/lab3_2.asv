% 定义系统开环传递函数
num = [1, 8];
den = conv(conv([1, 0], [1, 2]), [1, 8, 32]);
G = tf(num, den);

% (1) 绘制根轨迹
figure;
rlocus(G);
title('根轨迹图');

% (2) 计算两条分支进入右半平面和两条分支复数极点出发在实轴相交处的K值
s1 = 0 +  3.45i;  % 进入右半平面的根轨迹
s2 = 0 +  -3.45i;  % 进入右半平面的根轨迹
s3 = -10.2; %



% (3) 替换零点位置，并重新绘制根轨迹图
num_new = [1, -10];
G_new = tf(num_new, den);

figure;
rlocus(G_new);
title('替换零点后的根轨迹图');